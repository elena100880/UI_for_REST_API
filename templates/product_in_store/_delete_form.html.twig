<form method="post" action="{{ path('product_in_store_delete', {'id': product_in_store.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_in_store.id) }}">
    <button class="btn">Delete</button>
</form>
