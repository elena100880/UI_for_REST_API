{% extends 'base.html.twig' %}

{% block title %}ProductInStore{% endblock %}

{% block body %}
    <h1>ProductInStore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_in_store.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_in_store.name }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ product_in_store.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_in_store_index') }}">back to list</a>

    <a href="{{ path('product_in_store_edit', {'id': product_in_store.id}) }}">edit</a>

    {{ include('product_in_store/_delete_form.html.twig') }}
{% endblock %}
